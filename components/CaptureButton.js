import { View, Text, Button } from "react-native";
import React from "react";

import { takePictureAsync } from "expo-camera";

export default function CaptureButton() {
	return <Button title="Capture" onPress={() => {}} />;
}
